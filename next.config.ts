import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Uncomment if you want to disable Turbopack
  // experimental: {
  //   turbo: false,
  // },
  // Uncomment if CSS optimization causes lightningcss errors
  // experimental: {
  //   optimizeCss: false,
  // }
};

export default nextConfig;
